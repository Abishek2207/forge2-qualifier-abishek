# Forge 2 Edition 1 Qualifier

Hi, I'm Abishek R. I built this repository to participate in the Forge 2 Edition 1 qualifier challenge. This project demonstrates a multi-agent system coordinated through Slack to fulfill both the OpenClaw Mastery and Hermes Mastery requirements (Starter 1 and Starter 2), as well as a full React Kanban board built by the agents.

## Live Application
**Live URL**: https://forge2-kanban-abishek.vercel.app

## 🚀 Judge Quick Start

1. **Open Live URL**: `https://forge2-kanban-abishek.vercel.app`
2. **View screenshots folder**: Check `screenshots/` for Slack workflow proofs.
3. **Read agent-log.md**: Review the documented execution loop.
4. **Review SKILL.md**: Review the predefined skills in the `skills/` directory.
5. **Run tests**: Execute `pytest tests/` to verify project structure.
6. **Start Agents**: Run `python run_system.py` to start Hermes and OpenClaw.

---

## ✨ Project Highlights

* **Multi-agent architecture**: Orchestrator and Coder agents working in tandem.
* **Slack-based orchestration**: Full task delegation and reporting within Slack channels.
* **Persistent memory**: Hermes remembers past contexts and user preferences.
* **Local code execution**: OpenClaw securely writes and executes code locally.
* **Human review workflow**: Built-in pause for human validation before final delivery.
* **Live Kanban application**: Functional React + Vite app with drag-and-drop generated by the agents.
* **Free/open-source model stack**: Utilizing state-of-the-art open-weight models.

---

## 🏗️ Architecture Diagram

```ascii
User
  ↓
#sprint-main / #commands
  ↓
Hermes (Planning + Memory)
  ↓
#agent-orchestrator
  ↓
OpenClaw (Coding + Execution)
  ↓
#agent-log
  ↓
Hermes Validation
  ↓
#human-review
```

### Architecture & Roles

**Hermes (Brain / Orchestrator)**: It receives tasks, formulates a plan, checks its memory and skills, and delegates work to OpenClaw. It validates results before human review.
**OpenClaw (Hands / Coding Agent)**: It takes instructions from Hermes, generates code, runs it locally, and reports the results back to Slack.

### Slack Workflow
1. User posts a task in `#commands` or `#sprint-main`.
2. Hermes receives the task, generates a plan, and posts it to `#agent-orchestrator`.
3. OpenClaw executes the task, saving output locally, and posts a structured status report to `#agent-log`.
4. Hermes validates the execution and posts the final output to `#human-review`.

---

## 🛠️ Tech Stack & Models Used

* **Free Stack**: React, Vite, Node, LocalStorage, GitHub, Vercel/Netlify.
* **Hermes Model**: `owl-alpha` (Planning, memory, orchestration, task decomposition).
* **OpenClaw Model**: `qwen2.5-coder` via Ollama (Code generation, execution, file operations).
* **Fallback Models**: OpenRouter Free Models, Gemini 2.5 Flash.

---

## 📋 Qualifier Requirements Mapping

| Requirement          | Proof                     |
| -------------------- | ------------------------- |
| Starter 1 – OpenClaw | Slack execution loop      |
| Starter 2 – Hermes   | Planning + memory recall  |
| Coding Agent         | OpenClaw reports          |
| Orchestration        | Hermes planning           |
| Human Review         | #human-review screenshots |
| Deployment           | Vercel live URL           |
| Testing              | Pytest 6/6 passed         |

---

## 🔍 Evidence & Proofs

See `EVIDENCE.md` for a complete mapping of requirements to repository proofs. Additional specific proofs include:

* **Sprint Main task receipt**: `screenshots/`
* **Hermes planning proof**: `screenshots/`
* **Agent Orchestrator proof**: `screenshots/`
* **OpenClaw execution proof**: `screenshots/`
* **Agent Log proof**: `agent-log.md`
* **Human Review proof**: `screenshots/`
* **Memory recall proof**: `memory/hermes_memory.json`
* **Status report proof**: `agent-log.md`
* **Pytest execution proof (6/6 passed)**: Local execution
* **Live deployment proof**: `https://forge2-kanban-abishek.vercel.app`
* **GitHub repository proof**: Provided GitHub Link

---

## 📌 Kanban App Features

We built a tiny Trello-style Kanban board in the `frontend/` directory with the following features:
- [x] Boards
- [x] Lists inside a board: To Do, Doing, Done
- [x] Cards inside lists
- [x] Move a card between lists (Drag & Drop)
- [x] Card title + description editing
- [x] Colored tags/labels
- [x] Members and assign member to card
- [x] Due date and overdue visual flag

---

## 📂 Repository Structure

```text
forge2-qualifier-abishek/
├── agents/
├── frontend/
├── tests/
├── screenshots/
├── README.md
├── agent-log.md
├── SKILL.md
├── EVIDENCE.md
└── run_system.py
```

---

## 💻 How to Run Locally

### Prerequisites
1. Python 3.10+
2. Node.js 18+
3. A Slack workspace with a Bot Token and App-Level Token.
4. Local Ollama if using local models (`ollama pull qwen2.5-coder`).

### Installation
```bash
git clone https://github.com/Abishek2207/forge2-qualifier-abishek.git
cd forge2-qualifier-abishek

# Backend/Agent setup
python -m venv .venv
# Activate venv
pip install -r requirements.txt
```

### Configuration
Copy `.env.example` to `.env` and fill in your keys. Create the necessary Slack channels.

### Run the Agents
```bash
python run_system.py
```

### Run the Frontend
```bash
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173` to view the Kanban board.
